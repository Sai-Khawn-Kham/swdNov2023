// keyword
// ECMAScript5 -> var
// ECMAScript6 -> let, const

// declare variable
let x;

// assign value to variable
x = 5;
console.log(x);

// reassign value to variable
x = 7;
console.log(x);

const arr = [1, 2, 3, 4];
console.log(arr);
arr = 5;
console.log(arr);
//error occur because we cannot reassign constant variable
